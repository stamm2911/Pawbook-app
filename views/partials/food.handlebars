  <div>
      <img class="justify-start rounded-lg h-12 w-12 inline-block" src="{{profile.profile_picture}}" alt="">  
      <a class="inline-block ml-2 font-bold text-xl" href="">{{profile.profile_name}}</a>
      
      <h3 class="font-bold text-black-600 text-lg text-center">Brand: </h3>
      <p class="text-black-600 pt-2 text-center mb-8">{{food.food_brand}}</p>
      <h4 class="font-bold  text-black-600 text-lg pt-2 text-center">Quantity: </h4>
      <p class="text-black-600 pt-2 text-center mb-8">{{food.food_quantity}}</p>
      <p class="font-bold  text-black-600 pt-2 text-lg text-center ">Date:</p>
      <p class="text-black-600 pt-2 text-center mb-8">{{{food.food_date}}</p>
      <p class="font-bold  text-black-600 pt-2 text-lg text-center ">Food for:</p>
      <p class="text-black-600 pt-2 text-center mb-8">{{{food.food_type}}</p>
      <p class="font-bold  text-black-600 pt-2 text-lg text-center ">Location:</p>
      <p class="text-black-600 pt-2 text-center mb-8">{{{food.food_location}}</p>
      <button class="float-right text-5xl -mt-8"><i class="fas fa-paw"></i></button>

  </div>