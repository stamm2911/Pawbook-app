      <section>
            <h3 class="font-bold text-2xl">Welcome to Pawbook</h3>
            <p class="text-gray-600 pt-2">Sign in to your account.</p>
        </section>

        {{!-- <section class="mt-10"> --}}
            <form id="signInBtn" class="flex flex-col">
                <div class="mb-6 pt-3 rounded bg-gray-200">
                    <label class="block text-gray-700 text-sm font-bold mb-2 ml-3" for="email">Email</label>
                    <input type="text" id="email" class="bg-gray-200 rounded w-full text-gray-700 focus:outline-none border-b-4 border-gray-300 focus:border-blue-900 transition duration-500 px-3 pb-3">
                </div>
                <div class="mb-6 pt-3 rounded bg-gray-200">
                    <label class="block text-gray-700 text-sm font-bold mb-2 ml-3" for="password">Password</label>
                    <input type="password" id="password" class="bg-gray-200 rounded w-full text-gray-700 focus:outline-none border-b-4 border-gray-300 focus:border-blue-900 transition duration-500 px-3 pb-3">
                </div>
                <input class="bg-blue-900 hover:bg-blue-800 text-white font-bold py-2 rounded shadow-lg hover:shadow-xl transition duration-200" type="submit"></input>
            </form>
            {{!-- <button id="signInBtn" >boton</button> --}}
        {{!-- </section> --}}
    

    <div class="max-w-lg mx-auto text-center mt-12 mb-6">
        <p class="text-blue-900">Don't have an account? <a href="/signup" class="font-bold hover:underline">Sign up</a>.</p>
    </div>

<script type="text/javascript">
    const submitButton = document.getElementById('signInBtn');
    submitButton.addEventListener('submit', async (e) => {
        e.preventDefault();
        console.log('debug');
        console.log(document.getElementById('email').value.trim());
        console.log(document.getElementById('password').value.trim());
        let userSignIn = {
            email: document.getElementById('email').value.trim(),
            password: document.getElementById('password').value.trim()
        };
        userSignIn2 = JSON.stringify(userSignIn)
        console.log('string'+userSignIn2);
        const response = await fetch('/api/user/login', {
            method: 'POST',
            body: JSON.stringify(userSignIn),
            headers: { 'Content-Type': 'application/json' },
        });

        console.log(response)
        if (response.ok) {
        document.location.replace('/');
        } else {
        alert('Failed to log in.');
        }
      
    })
</script>
